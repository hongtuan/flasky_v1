# this requirements file is used by Heroku
# requirements for other configurations are located in the requirements subdirectory
# -r requirements/heroku.txt
# 1.豆瓣
# https://pypi.douban.com/simple

# 2.阿里云
# https://mirrors.aliyun.com/pypi/simple

# 3.清华大学
# https://pypi.tuna.tsinghua.edu.cn/simple

# 4.中国科技大学
# https://pypi.mirrors.ustc.edu.cn/simple
# ————————————————
# 版权声明：本文为CSDN博主「幽梦萧瑟」的原创文章，遵循CC 4.0 BY-SA版权协议，转载请附上原文出处链接及本声明。
# 原文链接：https://blog.csdn.net/mengyangcust/article/details/124334205
# 升级pip
# python -m pip install --upgrade pip -i https://pypi.tuna.tsinghua.edu.cn/simple
# 安装依赖包
# pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
-r requirements/dev.txt

# 在主库中，可以通过git checkout 切换到各章节代码
git checkout 5d
# 切换回来
git switch -
# 恢复代码
git reset --hard
git fetch --all
git fetch --tags
git reset --hard origin/master

# 5a版本，需要安装以下依赖
# 7a
flask
flask-bootstrap
flask-moment
flask-wtf
flask-sqlalchemy
flask-migrate
flask-mail
# 支持加载环境配置文件，需要安装的包
python-dotenv
# 链接MySQL数据，需要安装的包，并且将数据库配置修改为：mysql+pymysql://...
pymysql

# 一条命令安装
pip install flask flask-bootstrap flask-moment flask-wtf flask-sqlalchemy flask-migrate flask-mail -i https://pypi.tuna.tsinghua.edu.cn/simple

# 安装pip_search后，可以方便的查询python模块
pip_search pip_search
pip_search flask_days
